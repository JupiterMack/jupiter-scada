# Example environment variables for Jupiter SCADA
# Rename this file to .env and fill in your actual configuration values.

# --- OPC UA Server Connection ---
# The endpoint URL for the OPC UA server.
# Example: opc.tcp://localhost:4840/freeopcua/server/
OPCUA_SERVER_URL=opc.tcp://localhost:4840/

# Optional: Credentials for the OPC UA server if authentication is required.
# Leave blank if the server allows anonymous access.
OPCUA_USERNAME=
OPCUA_PASSWORD=

# --- Web Application (FastAPI/Uvicorn) ---
# Optional: Host and port for the web interface.
# Defaults to 0.0.0.0:8000 if not set.
# HOST=0.0.0.0
# PORT=8000